server {
    listen 80;
    server_name shorttermemail.com www.shorttermemail.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# SMTP configuration
server {
    listen 25;
    server_name shorttermemail.com;
    
    # SMTP proxy configuration would go here
    # This requires additional SMTP proxy setup
}